# yyl_sts2_mod — 设计评估与实现盘点

> 日期：2026-09-14 · 基准：你的设计草稿 + `MOD_DESIGN.md` + 当前代码
> 本轮改动已应用并通过构建（0 warning / 0 error），**尚未提交**

---

## 0. 本轮已做的修改

### 0.1 修好了「蓄势」卡面不显示

**根因**（不是数值 bug，是变量名不匹配）：

`WithPower<Powers.XuShi>(2, 1)` 生成的动态变量名取的是**类型名**，也就是 `XuShi`；
但 `cards.json` 里写的是 `{Qi:diff()}` —— 卡上根本没有叫 `Qi` 的变量，于是解析失败、数值不显示。

```
- "…下回合开始时获得{Qi:diff()}点炁。"
+ "…下回合开始时获得{XuShi:diff()}点炁。"
```

顺带把 `powers.json` 里蓄势的硬编码「N 点炁」改成 `{Amount}`（Watcher 也是这么写的）。

**这套命名规则值得记住**，以后加卡照着来就不会再犯：

| 代码写法 | 卡面占位符 |
|---|---|
| `WithDamage(...)` | `{Damage:diff()}` |
| `WithBlock(...)` | `{Block:diff()}` |
| `WithCards(...)` | `{Cards:diff()}` |
| `WithEnergy(...)` | `{Energy:diff()}` |
| `WithPower<Foo>(...)` | `{Foo:diff()}` ← **用类型名，不是你想要的资源名** |
| `new RepeatVar(...)` | `{Repeat:diff()}` |
| Power 自身描述引用层数 | `{Amount}` |

我用脚本把**全部 34 张卡**的代码声明与卡面引用做了一次交叉审计，确认蓄势是**唯一**的错配，其余都对得上。

### 0.2 修掉一个会毁掉游戏体验的问题

`LongHu`（龙虎山正一雷法）和 `Lucky`（运势）此前数值是 `WithDamage(700, 300)` / `WithBlock(700, 300)` —— 这两张是上轮被手动放回卡池的，也就是说**现在进游戏就可能抽到一张 700 伤害的普通卡**。

已连同 `RunAway`、`AWei`、`FengHou`、`OldFarm` 一起，把 `700/300` 中和为当前稀有度的合理值。

### 0.3 费用重排（按「低稀有 0 费、高稀有 2–3 费」）

| 卡牌 | 稀有度 | 原费 | 现费 |
|---|---|---|---|
| 吐纳 | Common | 1 | **0** |
| 蓄势 | Common | 1 | **0** |
| 狂热 | Uncommon | 1 | **0** |
| 炁流源体 | Rare | 1 | **2** |
| 天师度 | Rare | 1 | **2** |
| 天火 | Rare | 1 | **2** |
| 投喂 | Rare | 1 | **2** |
| 大啖食粮 | Rare | 1 | **2** |
| 黑色幽默 | Rare | 1 | **2** |
| 金光咒 | Basic | 2 | 2（数值 `12→18` 改 **`12→16`**，对齐草稿） |

当前费用分布：**0 费 3 张 / 1 费 20 张 / 2 费 7 张 / 3 费 3 张**。

1 费仍占绝大多数，这是接下来要继续削的部分——静默猎手的特征是 0 费牌很多，1 费牌应该只占一半左右。

### 0.4 其他

- 补齐主仓库剩余 6 处 `[Pool]` 替换（Ancient2 / RunAway / AWei / FengHou / NlFoot / OldFarm）
- 把 3 处 `asd` 占位文案按实际效果重写

---

## 1. 实现进度

| 目标 | 计划 | 现状 | 完成度 |
|---|---|---|---|
| 卡牌 | 88 | 33（含 Token 2） | **38%** |
| 遗物 | 6 | 3（黄桃罐头 / 心魔 / 大瓶黄桃罐头） | 50% |
| 药水 | 2–4 | **0**（只有一个模板基类） | 0% |
| 事件 | 2 | **0** | 0% |
| Power | 6 | 6 | 100% |
| 姿态 | — | 8（含逆生三重） | — |

**已完整实现的体系**：

- 炁管线（`GainQi`/`LoseQi` + `IGainQi`/`ILoseQi` 钩子链）—— 这套写得很干净，是本 mod 最扎实的部分
- 产炁 5 张：吐纳 / 蓄势 / 炁流源体 / 天师度 / 马步 ✅
- 耗炁 5 张：掌心雷 / 移穴 / 天火 / 散炁 / 通畅 ✅
- 数值能力 3 个：温养 / 丹噬 / 老农功 ✅
- 奶龙 7 张：指认 / 心防 / 破防 / 狂热 / 投喂 / 大啖食粮 / 黑色幽默 ✅
- 能力 6 个：炁 / 逆生 / 天师度 / 温养 / 丹噬 / 性命双全 ✅
- 初始牌 4 张：打击 / 防御 / 金光咒 / 阳五雷 ✅
- 逆生三重已改为姿态链（一重→二重→三重继承）✅

**完全没动的**：药水、事件、遭遇、电磁场体系。

---

## 2. 草稿与实现的偏差（需要你拍板）

这几条代码和草稿对不上，我不敢擅自改设计，列出来给你定：

| # | 项 | 草稿 | 现在的实现 |
|---|---|---|---|
| 1 | **阳五雷** | 1 费 **8 伤害**，额外造成「金光护体层数」次伤害，**失去 1 点炁** | 1 费、**对全体** 5 次 1 点伤害（=5 点），上易伤+虚弱，有金光护体时 Damage+1；**不失去炁** |
| 2 | **天师度** | 能力表写「失去 **3** 点炁」，卡牌表写「失去 **2** 点炁」 | 代码 `BaseQiLoss = 2`（草稿自相矛盾） |
| 3 | **金光护体** | 受到的伤害减少 **10%** | 每层减少 **2 点**固定值 |
| 4 | **黄桃罐头** | 击杀奶龙回复 **4** 点生命 | `NlPower` 回复 **3** 点 |
| 5 | **心魔（先古）** | 获得 **5** 点炁；心魔 buff 施加时**调用声音接口发出奶龙的声音** | 拆成了「心魔 NlCan2」+「大瓶黄桃罐头」两个遗物；**声音接口未实现** |
| 6 | **狂热** | 措辞像持续效果「攻击奶龙时额外 +4」 | 实现为一张攻击牌：若目标是奶龙则本次 +4 |
| 7 | **掌心雷** | 草稿里描述被截断（「失去1点炁，」后面没了） | 实现为 6→8 伤害 |

---

## 3. 设计评价：三个我认为必须处理的结构风险

### 风险一：散炁 + 马步 = 无限能量（最严重，建议优先修）

算一下：

- 马步 1 费 → 产 1~2 炁
- 散炁 1 费 → 耗 1 炁、得 2~3 能量

循环一轮「马步 + 散炁 ×2」= 花 3 费、产 2 炁又耗 2 炁、**净赚 1~3 能量**。炁还没少，能量却涨了。
再叠上吐纳 0 费产炁、通畅抽牌，很容易滚成无限。

**建议**（任选）：散炁改为「消耗」；或加「每回合只能用一次」；或改成「失去 1 炁，获得 2 能量，抽 1 张」并固定 1 费不让降费。

### 风险二：炁是无上限乘算，后期会爆炸

炁每点 +10% 是**乘算**，10 点 = 2.0×，20 点 = 3.0×。
大瓶黄桃罐头再叠 20%/点，5 炁时就是 1.5 × 2.0 = **3.0×**。
天火在 10 炁时是 140~180 点群体伤害。

对标静默猎手的小刀是**线性**叠加，炁是**指数**增长，这两者的成长曲线完全不同量级。

**建议**：给炁加软上限（如 10 点后收益减半），或给天火加「最多结算 5 点炁」。

### 风险三：初始遗物让整个条件体系失效

黄桃罐头的隐藏能力是「所有敌人视作奶龙」。那么：

- 指认的「如果对方是奶龙」永远成立
- 破防的「使奶龙失去力量」永远成立
- 狂热的「对奶龙 +4」永远成立

也就是说**初始遗物把所有条件卡变成了无条件强卡**，条件设计本身失去意义，而且这还是开局白送的。

**建议**：要么给这个隐藏能力加代价（比如「敌人对你造成的伤害 +X%」），要么把它挪到需要先古遗物才解锁。

### 次要问题

- **心防** 与草稿里的「破损的奶龙玩偶」（减伤 30%）功能重叠，建议错开
- **逆生三重是姿态**，会覆盖平静/愤怒/神性；而草稿里 4 个 stance 是既有支撑 —— 需要确认这两套是否应该互斥
- **6 张未设计占位卡**（龙虎山/运势/逃离/阿威/风后/老农功卡）现在都在池里，其中阿威/风后/老农功卡数值完全相同（都是 8 伤 + 8 格挡）。建议正式发布前设计出差异化，或先移出卡池

---

## 4. 之后的安排（建议分四阶段）

**阶段一：堵住漏洞（1–2 天，优先做）**
1. 修散炁无限能量（§3 风险一）
2. 给炁加软上限 / 给天火加结算上限（§3 风险二）
3. 确认黄桃罐头隐藏能力的定位（§3 风险三）
4. 拍板 §2 的 7 条偏差，尤其是**阳五雷**（草稿与实现差得最远）

**阶段二：补齐骨架（1 周）**
5. 2–4 瓶药水（`Potions/` 目前只有模板）
6. 2 个事件（参考 LexNinja2 的 `Event/` 结构）
7. 补齐 6 个遗物中的另外 3 个：普通 ×1、罕见（破损的奶龙玩偶）×1、稀有 ×1
8. 心魔 buff 的奶龙声音接口（草稿明确要求，也是这个梗的点睛之笔）

**阶段三：扩充卡池到 88 张（2–3 周，工作量最大）**
9. 先按体系补齐：炁体系目前 10 张，需要更多低费 cycle 牌
10. 奶龙体系目前 7 张，可以再扩
11. 给 6 张占位卡设计差异化效果
12. 继续削 1 费牌占比，向静默猎手的 0 费密集靠拢

**阶段四：资源与打磨**
13. 实际制作 OGG / PNG / TSCN（目前全靠 `yylAudio`/`yylAnim` 安全跳过）
14. 补 zhs 本地化（现在只有 eng）
15. 游戏内完整战斗回归

---

---

## 5. 英文名改造对照表（已全部应用）

命名原则：不逐字直译，取能反映意象的英文词。

### 卡牌

| 原（拼音） | 现（英文） | 中文 |
|---|---|---|
| yylStrike / yylDefend | `YylStrike` / `YylDefend` | 打击 / 防御 |
| GoldenWaveCard | `GoldenCharm` | 金光咒 |
| YangThunder | `SolarThunder` | 阳五雷 |
| Ancient1 | `LunarThunder` | 阴五雷 |
| Ancient2 | `WardingBlade` | 辟邪剑法 |
| TuNa | `QiBreath` | 吐纳 |
| XuShi | `GatherMight` | 蓄势 |
| MaBu | `HorseStance` | 马步 |
| XinFang | `HeartWard` | 心防 |
| ZhiRen | `Identify` | 指认 |
| LongHu | `DragonTigerRite` | 龙虎山正一雷法 |
| Lucky | `Fortune` | 运势 |
| RunAway | `Escape` | 逃离 |
| KuangRe | `Zeal` | 狂热（已改为能力牌） |
| PoFang | `GuardBreak` | 破防 |
| NlFoot | `NailongKick` | 奶龙无影脚 |
| AWei | `EighteenForms` | 阿威十八式 |
| FengHou | `WindArray` | 风后奇门 |
| OldFarm | `PeasantDrill` | 老农功（卡） |
| SanQi | `QiDispersal` | 散炁 |
| TongChang | `FreeFlow` | 通畅 |
| YiXue | `AcupointShift` | 移穴 |
| QiLiuYuanTi | `QiWellspring` | 炁流源体 |
| TianShiDuCard | `HeavenlyRite` | 天师度 |
| TianHuo | `SkyFire` | 天火 |
| TouWei | `Offering` | 投喂 |
| DaDanShiLiang | `Devour` | 大啖食粮 |
| HeiSeYouMo | `DarkHumor` | 黑色幽默 |
| ReverseLife1/2/3 | `RebirthOne/Two/Three` | 逆生一/二/三重 |
| —（新增） | `QiTransmute` | 炁化金光 |
| —（新增） | `GoldenWardCard` | 金光护体源 |

### 能力 / 姿态 / 遗物

| 原 | 现 | 中文 |
|---|---|---|
| Qi | `Qi`（保留，本就是英文） | 炁 |
| GoldenWave | `GoldenAegis` | 金光护体 |
| OldFarm | `RusticRoot` | 老农功（能力） |
| WenYang | `Nurture` | 温养 |
| DanShi | `CinnabarBite` | 丹噬 |
| XuShi | `StoredMight` | 蓄势（能力） |
| TianShiDu | `HeavenlyBurden` | 天师度（能力） |
| XinFang | `HeartGuard` | 心防（能力） |
| XingMingShuangQuan | `TwinPerfection` | 性命双全 |
| NlPower / NlPowerPlus | `NailongMark` / `InnerDemon` | 奶龙 / 心魔 |
| —（新增） | `Fervor` | 狂热（能力） |
| —（新增） | `GoldenWard` | 金光护体源（能力） |
| ReverseLife | `InverseLife` | 逆生 |
| ReverseLife1/2/3Stance | `RebirthStanceOne/Two/Three` | 逆生一/二/三重（姿态） |
| NlCan1 / NlCan2 | `PeachCan` / `BigPeachCan` | 黄桃罐头 / 大瓶黄桃罐头 |

> 本地化键 `YYL_STS2_MOD-<SNAKE_CASE>` 已同步重建，`cards.json` / `ancients.json` / `powers.json` / `relics.json` 全部对齐（STS001 分析器校验通过）。

---

## 6. 提醒

- 改动都还在**工作区未提交**
- 主仓库还有个未跟踪目录 `_deleted_2026_ritsu_migration/`（内含 `yylRitsuCard.cs.bak`），和一批未跟踪的 `.uid` 文件，提交前记得决定怎么处理
- 我上一轮在 worktree 里做的改动与主仓库已分叉，**以主仓库为准**
