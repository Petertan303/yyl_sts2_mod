# yyl_sts2_mod 实现报告

> 日期：2026-09-14
> 基准：`MOD_DESIGN.md`、`G:\Git\WatcherMod`，音画方案参考 `G:\Git\LexNinja2`。
> 本次仍只依赖 BaseLib；没有引入 STS2RitsuLib。

## 1. 本次修复结论

本次修复的重点不是只补卡牌效果，而是把卡牌动态变量、命令调用和结算顺序统一到
BaseLib 的标准流程：

- 所有正式卡牌已统一继承项目内的 `yylCardModel`，卡图路径和缺图回退规则一致。
- 卡面数值改为使用 `WithDamage`、`WithBlock`、`WithPower`、`WithCards`、
  `WithEnergy`、`RepeatVar` 等动态变量，升级值也会随之更新。
- 攻击牌统一优先使用 `CommonActions.CardAttack`，使伤害修正、攻击动画、
  命中结果、击杀结算和奶龙回血能够进入同一条流程。
- 格挡、抽牌、能量、选牌分别使用 `CommonActions.CardBlock`、
  `CommonActions.Draw`、`PlayerCmd.GainEnergy`、`CommonActions.SelectSingleCard`。
- 未完成且会污染卡池的占位牌已暂时移出正式卡池，不再以异常数值进入游戏。

## 2. 卡牌

### 2.1 基础牌

| 卡牌 | 当前实现 |
|---|---|
| 打击 | 1 费，造成 6 伤害，升级后 9 伤害。 |
| 防御 | 1 费，获得 5 格挡，升级后 8 格挡。 |
| 阳五雷 | 对所有敌人施加 1 层易伤和虚弱，造成 5 次伤害；金光护体存在时额外追加伤害。 |
| 金光咒 | 1 费，获得 12 格挡和 1 层金光护体，升级后 18 格挡。 |

### 2.2 炁体系

| 卡牌 | 当前实现 |
|---|---|
| 吐纳 | 1 费，获得 1 至 2 点炁，抽 1，消耗。动态变量为 `Qi` 和 `Cards`。 |
| 蓄势 | 1 费，施加 2 至 3 点延迟炁，下个玩家回合开始时结算并移除 Power。 |
| 炁流源体 | 1 费，获得等同于当前炁量的炁，即翻倍；0 炁时无效果；消耗。 |
| 天师度卡 | 1 费，获得 10 炁、3 层金光护体，并施加每回合结束失去 2 炁的 `TianShiDu`。 |
| 马步 | 1 费，获得 3 至 5 格挡和 1 至 2 点炁。 |
| 掌心雷 | 1 费，先失去 1 点炁，再造成 6 至 8 点标准攻击伤害。 |
| 移穴 | 1 费，获得 10 至 15 格挡，失去 1 炁，从抽牌堆选择 1 张牌加入手牌。 |
| 天火 | 1 费，失去当前全部炁，对每个敌人造成“实际失去炁量 × 14 至 18”的标准攻击伤害。 |
| 散炁 | 1 费，失去 1 炁，获得 2 至 3 点能量。 |
| 通畅 | 1 费，失去 1 炁，抽 3 至 4 张牌。 |

### 2.3 奶龙体系

| 卡牌 | 当前实现 |
|---|---|
| 指认 | 1 费，造成 6 至 9 点伤害；攻击前记录目标奶龙身份，若为奶龙则获得 1 能量。 |
| 心防 | 1 费，获得 3 至 5 格挡，并获得持续到本回合结束的 `XinFang` Power，受到奶龙伤害减半。 |
| 破防 | 1 费，造成 6 伤害 2 至 3 次；若目标是奶龙，每次实际命中使其失去 1 点力量；消耗。 |
| 狂热 | 1 费，造成 4 点标准攻击伤害；目标是奶龙时本次追加 4 点伤害。 |
| 投喂 | 1 费，给予所有存活奶龙 10 点格挡，获得 2 至 3 点炁和 2 点能量。 |
| 大啖食粮 | 1 费，对所有存活奶龙造成 4 至 6 点标准攻击伤害，并按实际未格挡伤害治疗自身。 |
| 黑色幽默 | 1 费，为所有奶龙回复 20 生命，获得 3 点炁和 1 层无实体。 |

### 2.4 逆生态

| 卡牌 | 当前实现 |
|---|---|
| 逆生一重 | 3 费，进入“逆生一重”姿态，并将逆生二重加入手牌；消耗。 |
| 逆生二重 | 3 费 Token，进入“逆生二重”姿态，并将逆生三重加入手牌；消耗；升级降费。 |
| 逆生三重 | 3 费 Token，进入“逆生三重”姿态。 |

原 `ReverseLife` Power 文件仍保留为历史代码，但正式卡牌已不再向玩家施加该 Power。

### 2.5 暂时禁用

以下占位牌已移除 `[Pool]`，不会进入正式卡池：

- 龙虎山正一雷法、运势、逃离、阿威十八式、风后奇门。
- 老农功卡、奶龙无影脚。
- 阴五雷、辟邪剑法。

这些文件暂未删除，便于后续补完；它们仍可编译，但不会作为正式掉落或起始卡出现。

## 3. Power

| Power | 当前实现 |
|---|---|
| 炁 `Qi` | Counter；每层使造成的伤害增加 10%。 |
| 金光护体 `GoldenWave` | Counter；每层使受到的普通伤害最多降低 2 点，且不会把伤害减成负数。 |
| 老农功 `OldFarm` | 每次获得炁时额外增加等同于层数的炁。 |
| 温养 `WenYang` | 炁获取结算完成后，对每个敌人造成 4 × 层数伤害。 |
| 丹噬 `DanShi` | 炁损失结算完成后，获得 4 × 层数格挡。 |
| 蓄势 `XuShi` | 玩家下回合开始时获得 Amount 点炁，然后移除自身。 |
| 天师度 `TianShiDu` | 玩家回合结束时失去 2 × 层数炁。 |
| 心防 `XinFang` | 本回合来自奶龙的伤害乘以 0.5，玩家回合结束时移除。 |
| 性命双全 `XingMingShuangQuan` | 让下一张由持有者打出的牌额外结算 1 次，然后移除自身。 |
| 奶龙 `NlPower` | 奶龙标记，并记录施加者；施加者攻击并击杀该目标时回复 3 生命。 |
| 心魔 `NlPowerPlus` | 所有人视作奶龙的高级标记；施加者攻击并击杀目标时回复 6 生命。 |

`NlPower` 和 `NlPowerPlus` 已改为 `InstancedPerApplier`，并对攻击状态做结束复位，
避免不同施加者共享实例或战斗中的状态残留。

## 4. 遗物

| 遗物 | 当前实现 |
|---|---|
| 黄桃罐头 `NlCan1` | 第一回合抽牌前获得 3 炁，并把所有敌人视作奶龙。 |
| 炁体源流 `NlCan2` | 第一回合抽牌前把玩家、队友和敌人视作奶龙；击杀奶龙按 `NlPowerPlus` 回复 6 生命。 |
| 大瓶黄桃罐头 | 第一回合抽牌前获得 5 炁并把所有人视作奶龙；每点炁额外使自身造成的伤害增加 20%。 |

战斗开始类遗物使用“第一回合 `BeforeHandDraw`”触发，避免上一场战斗的 SpireField
状态跨战斗残留。

## 5. 姿态

| 姿态 | 当前实现 |
|---|---|
| 无姿态 | 默认状态，不参与战斗 Hook。 |
| 平静 | 退出时获得 2 点能量，可通过 Hook 修改。 |
| 愤怒 | 自身造成和受到的非无来源伤害翻倍。 |
| 神性 | 自身造成的非无来源伤害变为三倍，进入时获得 3 点能量。 |
| 逆生一重 | 玩家每回合第一次获得格挡时，该次格挡翻倍。 |
| 逆生二重 | 保留一重效果，玩家回合开始时回复 6 生命。 |
| 逆生三重 | 保留二重效果，玩家回合开始时获得 1 层无实体。 |

逆生态通过姿态 Hook 实现，避免原先 Power 与卡牌 Token 链混用造成的结算顺序问题。

## 6. 音画与资源规范

### 6.1 `yylAudio`

- 音效：`res://yyl_sts2_mod/audio/sfx/<分类>/<名称>.ogg`
- 环境音：`res://yyl_sts2_mod/audio/ambience/<名称>.ogg`
- 语音：`res://yyl_sts2_mod/audio/voice/<名称>.ogg`
- 支持 `PlaySfx`、`PlayVoice`、`PlayLooped`、`Stop`、`StopAll`。
- 音频会缓存、遵循游戏音效音量，并在资源缺失时只记录一次警告后安全跳过。

### 6.2 `yylAnim`

- 继续使用 `AnimatedSprite2D` 帧动画，不引入 Spine 依赖。
- 动画名固定为 `attack`、`cast`、`hurt`、`die`、`idle_loop`。
- 提供攻击、施法、受击、死亡触发入口，并兼容当前角色场景的动画命名。
- 姿态切换播放 `cast`，姿态视觉先采用 `AnimatedSprite2D.Modulate` 着色。
- 逆生一重、二重、三重分别使用偏绿、青绿、金色的姿态着色。

### 6.3 VFX

- 姿态或技能场景：`res://yyl_sts2_mod/scenes/vfx/<名称>.tscn`
- VFX 图片：`res://yyl_sts2_mod/images/vfx/<名称>.png`
- 缺少场景或图片时会安全跳过，不再因空资源直接进入异常流程。

当前仓库只完成了代码入口和路径规范，实际 OGG、PNG、TSCN 资源尚未补入。

## 7. 验证与已知风险

已验证：

- `dotnet build yyl_sts2_mod/yyl_sts2_mod.csproj --no-restore`
- 结果：0 warning，0 error。
- `cards.json`、`powers.json`、`relics.json` 均可正常解析。
- 关键 BaseLib API 已通过本机程序集反射确认。

仍需后续处理：

- 尚未进行完整游戏内战斗回归，尤其是多段攻击、护盾结算和队友联机场景。
- `NlPower` / `NlPowerPlus` 仍同时承担标记和击杀奖励职责，后续可拆成纯 Tag 与奖励 Power。
- 天火、通畅、天师度卡等数值仍有明显平衡风险，需要实机试玩调整。
- 狂热当前按“打出狂热时目标为奶龙则追加伤害”实现；若设计要改成持久 Power，需要另加 Power。
- 破防按实际命中的攻击结果扣力量；目标提前死亡时不再继续处理后续命中。
- 所有计划中的音频和 VFX 资源仍待制作，当前代码会安全跳过缺失资源。
